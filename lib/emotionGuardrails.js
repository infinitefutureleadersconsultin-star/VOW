export function checkEmotionGuardrails(logs){const alerts=[];const now=new Date();const last7=logs.filter(l=>(now-new Date(l.timestamp))/86400000<=7);const anxious=last7.filter(l=>l.emotion==="anxious").length;if(anxious>=3)alerts.push({type:"concern",message:"Youve felt anxious 3x this week."});return alerts}
